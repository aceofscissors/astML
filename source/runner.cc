#include <astML/astML.hh>

namespace astmlib {

auto runner::parse_from(const std::string_view &path) -> astml_doc {
  // TODO(Lavinia)
}

auto runner::generate_to(const astml_doc &doc,
                         const std::string_view &path) -> void {
  // TODO(Lavinia)
}

} // namespace astmlib
